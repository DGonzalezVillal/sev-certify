# Target reached once reporting is complete
# Final services should use: WantedBy=stop.target

[Unit]
Description=Stop
After=report.target
Requires=report.target

[Install]
WantedBy=multi-user.target
